apiVersion: operators.coreos.com/v1
kind: OperatorSource
metadata:
    name: kubestone
    namespace: openshift-marketplace
spec:
    type: appregistry
    endpoint: https://quay.io/cnr
    registryNamespace: "xridge"
    displayName: "Marketplace Operators"
    publisher: "xridge.io"
